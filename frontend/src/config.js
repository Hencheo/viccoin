/**
 * config.js - Arquivo de configuração global
 * 
 * Este arquivo contém configurações que podem ser usadas em toda a aplicação
 */

// URL base da API (altere para a URL do seu servidor)
export const API_URL = 'https://viccoin.onrender.com';

// Configurações de timeout para requisições (em ms)
export const API_TIMEOUT = 15000; // 15 segundos para compensar latência no Render

// Versão do aplicativo
export const APP_VERSION = '1.0.0';

// Nome da aplicação
export const APP_NAME = 'VicCoin'; 